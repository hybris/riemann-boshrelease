(def graph
  <% if_p("riemann.graphite.host", "riemann.graphite.port", "riemann.graphite.pool") do |host, port, pool| %>
  (
    graphite {
      :host "<%= host %>",
      :port <%= port %>,
      :pool-size <%= pool %>
    }
  )
  <% end %>
)
