(info "Loading health monitor alerts")

(defn hm-alerts [pd]
  (info "Setting up health monitor alerts")
  (try
    (where
      (and (service "bosh.hm") (state "alert"))
      (:trigger pd))
  (catch Exception e #(warn "clamav-exception: " e))))
