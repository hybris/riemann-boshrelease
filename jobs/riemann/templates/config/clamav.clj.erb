(info "Loading clamav alerts")

(defn clamav-alerts [pd]
  (info "Setting up clamav alerts")

  (try
    (where
      (and (service "clamav") (state "critical"))
      (:trigger pd))
    (catch Exception e #(warn "clamav-exception: " (.getMessage e)))
  ))
