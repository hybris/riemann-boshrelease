(def email
  (
    mailer {
      :from "<%= p('riemann.mailer.from') %>"
      :host "<%= p('riemann.mailer.smtp-server') %>"
      :port <%= p('riemann.mailer.smtp-port') %>
      :user "<%= p('riemann.mailer.smtp-user') %>"
      :pass "<%= p('riemann.mailer.smtp-password') %>"
    }
  )
)

(def tell-idefix    (throttle 5 3600  (email "team-idefix@sap.com" "michael.stephan@sap.com")))
(def tell-framefrog (throttle 5 3600  (email "team-framefrog@sap.com")))
(def tell-predictor (throttle 5 3600  (email "team-predictor@sap.com")))
(def tell-wombats   (throttle 5 3600  (email "team-wombats@sap.com")))
